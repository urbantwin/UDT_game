// Layer composition for map and overlays.
// TODO:
// - Order: map background -> geometry -> user location.
// - Pass view/projection info to each layer.

export function createLayers() {
  return [];
}
