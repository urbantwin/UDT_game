// Render loop orchestrator.
// TODO:
// - Setup requestAnimationFrame loop.
// - Render map background then overlays in correct order.

export function createRenderer() {
  return {
    start() {},
    stop() {}
  };
}
