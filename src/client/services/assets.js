// Asset loading helpers for map data (tiles, metadata).
// TODO:
// - Preload critical tiles or base map image.
// - Expose progress and caching.

export function preloadMapAssets() {
  return Promise.resolve();
}
