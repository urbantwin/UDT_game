// App bootstrap: wire map, overlays, input, and geolocation together.
// TODO:
// - Create rendering context and attach to DOM.
// - Initialize map view and data sources.
// - Register input handlers (pan/zoom, selection).
// - Start render loop.
// - Start geolocation watcher and pass updates to overlay/state.

export function bootstrapApp() {
  // Intentionally left as a stub for now.
}
