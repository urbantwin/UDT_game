// Map configuration.
// TODO:
// - Define map bounds (min/max lat/lon).
// - Define tile size, max/min zoom.
// - Define base tile URL template or local map asset path.

export const mapConfig = {
  bounds: null,
  minZoom: 0,
  maxZoom: 18,
  tileSize: 256,
  tileUrl: ""
};
