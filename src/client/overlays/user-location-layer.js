// Overlay for user's geolocation (red dot).
// TODO:
// - Render a red dot at the user's map position.
// - Optionally render accuracy radius.

export function createUserLocationLayer() {
  return {
    setLocation() {},
    render() {}
  };
}
