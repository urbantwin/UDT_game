// Overlay for interactive geometric elements.
// TODO:
// - Store and render shapes (polygons, lines, points).
// - Implement hover/selection states.
// - Provide hit-test helpers for interaction.

export function createGeometryLayer() {
  return {
    setGeometries() {},
    render() {}
  };
}
