# Map Assets

This folder will contain georeferenced map assets or tile data.
- Put local tiles under a subfolder like `tiles/{z}/{x}/{y}.png`.
- Or include a single georeferenced image plus metadata.
