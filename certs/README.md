Development HTTPS certificates are generated locally.

Do not commit private keys.

Steps:
1. Run `npm run https:setup`
2. Run `npm run dev:https` (or `npm run dev:full:https`)
3. Open `https://<your-lan-ip>:5173` on your phone
